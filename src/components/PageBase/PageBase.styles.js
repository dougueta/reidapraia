import styled from 'styled-components'
import theme from '../../theme'

export const Page = styled.div`
  max-width: 100%;
  height: 100vh;
  padding: 10px;
  background-color: ${theme.palette.background.default};
  /* TODO: ver como setar o pagind da pagina sem afetar o menu */
  /* padding-top: 64px; */
  /* height: 100vh; */
`
